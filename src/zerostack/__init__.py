"""Zerostack: a zero cost AI architecture baseline.

The package is organised as the eight layers described in ARCHITECTURE.md. Every
layer exposes a small protocol plus at least one implementation that runs with no
external service, so a clean clone is runnable before any infrastructure exists.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
